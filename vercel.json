{
  "buildCommand": "bash scripts/prepare-vercel.sh && prisma generate && prisma migrate deploy && next build",
  "devCommand": "next dev",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "regions": ["hkg1"]
}
